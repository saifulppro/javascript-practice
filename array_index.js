var friendAge = [12, 13, 14, 15, 16, 17, 18];
// other ways
var shiblu = friendAge[5];
// change array values
friendAge[2] = 21;
// position of arrays
var position = friendAge.indexOf(15);
var position2 = friendAge.indexOf(18);

// advance level of array

// add a value in array
friendAge.push(19);
friendAge.push(20);
// out last value in array
friendAge.pop();
// out of a target value in array

friendAge.pop(19)

// add elements from beginning of array
friendAge.unshift(21);

// remove first elements from beginning of array

friendAge.shift()

var tealine = ['Kamal', 'Salam', 'Balam', 'Justien Bieber'];
tealine.push('Riman');
// 2 ,3 numer array cutting
var part = tealine.slice(2, 4);
console.log(part);

// console.log(tealine);